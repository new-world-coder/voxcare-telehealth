apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: voxcare
  labels:
    app: mongo
    component: database
spec:
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongo
  selector:
    app: mongo
  type: ClusterIP
